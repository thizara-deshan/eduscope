# F-05 systemd verification

F-05 performs static unit syntax, dependency graph, and hardening verification.
The live restart and degradation witness is `NOT RUN` until F-08 installs the
release artifacts and rendered units on the staging target, as required by the
approved plan amendment.
