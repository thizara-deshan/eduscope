# Device installation

Run `install.sh` as root with absolute paths. Keep `provisioning.json` and
`bootstrap-admin.password` beside the secrets file. Run `--dry-run` first.
Demo staging requires the acknowledgement flag and is never F-09 evidence.
