/** Gentle copy — intentionally no `0 points` / rank dash. */
export function NoParticipationMessage() {
  return <p className="mt-4 text-base text-muted">You didn&rsquo;t answer any questions this session.</p>;
}
