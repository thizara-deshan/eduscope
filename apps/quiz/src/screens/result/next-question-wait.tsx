/** The dominant instruction on S-40 — store transitions replace the screen, never a timer. */
export function NextQuestionWait() {
  return <p className="result-wait">Waiting for the next question</p>;
}
