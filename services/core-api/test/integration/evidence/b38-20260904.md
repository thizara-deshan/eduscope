# B-38 core-api gate evidence

Records the green `gate:core-api` run performed for the Workstream E
prerequisite (item 2: runner + package script + a green live run). This is a
real gate run, not a template. Field-level `device-smoke.test.ts` values are
asserted by that test (it passed) but its per-run `b38-evidence.json` is written
to an ephemeral stack dir that the harness removes on teardown, so those
per-run values are not persisted here and are not invented below.

## Identity

| Field | Value |
|---|---|
| Core commit | `04af52faebc0b501c6c3aa440f3fa499cd643825` |
| Host | `edus-desktop` (Radxa ROCK 5 ITX; Linux 6.1.0-1025-rockchip aarch64) |
| Date | 2026-09-04, Asia/Colombo |
| Command | `pnpm --filter @eduscope/core-api gate:core-api` |
| Exit code | 0 |

## Gate steps and results

| Step | Result |
|---|---|
| `@eduscope/core-api typecheck` | PASS (no `error TS`) |
| `@eduscope/core-api test` | PASS — 83 test files, 682 passed / 1 skipped |
| `@eduscope/shared test` | PASS — 6 test files, 58 passed |
| `@eduscope/api-client test` | PASS — 30 test files, 297 passed |
| Ownership marker printed | `79 REST / 22 panel events / 5 preview variants / 1 sync.hello` |

The `{"level":40,...}` lines in the run log are intentional warn-level logs
emitted by negative-path tests (helper `smart.read` failures, upload
failure-class paths, pm-bridge disconnect, LED update failures during forced
error scenarios). They are asserted behaviors, not gate failures; every test
file passed.

## device-smoke.test.ts

| Field | Recorded value |
|---|---|
| Result within suite | PASS (also re-run standalone: `test/integration/device-smoke.test.ts` — 1 passed) |
| HTTP status sequence | Asserted by test (PASS); per-run values ephemeral, not persisted by harness |
| Panel WS event names/seqs | Asserted by test (PASS); per-run values ephemeral |
| Lecture session / recording / segment ids | Asserted by test (PASS); per-run values ephemeral |
| Segment paths and byte sizes | Asserted by test (PASS); per-run values ephemeral |
| First graceful-stop lifecycle result | Asserted by test (PASS) |
| PM record survived; no PM stop on shutdown | Asserted by test (PASS) |
| Restart adoption snapshot | Asserted by test (PASS) |
| Authenticated media 206 and Content-Range | Asserted by test (PASS) |
| Selected USB device and copied bytes | Asserted by test (PASS) |
| Upload checkpoint / resumed offset / final bytes | Asserted by test (PASS) |
| Duplicate upload/answer projection count | Asserted by test (PASS) |
| Cross-AuthSession private-event check | Asserted by test (PASS) |
| Logout REST/WS revocation check | Asserted by test (PASS) |
| Ownership totals (REST/panel/preview/sync) | 79 / 22 / 5 / 1 (gate marker) |
| Open-handle/teardown result | Clean — gate exited 0 with no unhandled rejection reported |

To persist the field-level `b38-evidence.json`, re-run device-smoke with a
harness change that keeps `stack.dir`; not required for the Workstream E
prerequisite, which is satisfied by this green gate run.

## Verdict

PASS — `gate:core-api` green on core commit `04af52f`, 2026-09-04.
